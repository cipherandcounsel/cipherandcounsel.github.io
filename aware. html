<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Aware — Digital Footprint — Cipher &amp; Counsel</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,700;0,800;1,400;1,700&family=IBM+Plex+Mono:wght@300;400;500&family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">
<style>


/* ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   CIPHER & COUNSEL — STANFORD AESTHETIC
   
   PALETTE:
   Background:  #000000  pure black — no warmth, no gradient
   Primary:     #C9A84C  antique gold — the only color that matters
   Text:        #F0EDE6  warm off-white — not pure white, never harsh
   Muted:       #6B7280  cool gray — supporting text
   Border:      rgba(201,168,76,0.15) — barely-there gold
   Accent line: #C9A84C  gold only — never blue, never red
   
   TYPOGRAPHY:
   Headlines:   Playfair Display — serif authority
   Labels:      IBM Plex Mono — technical precision  
   Body:        Inter — modern legibility
   
   PHILOSOPHY:
   One background. One accent. Maximum restraint.
   Every element earns its place.
   Nothing decorative. Everything intentional.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ */

*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
html { scroll-behavior: smooth; background: #0D1B2A; }

body {
  background: #0D1B2A !important;
  color: #F0EDE6 !important;
  font-family: 'Inter', sans-serif;
  font-size: 16px;
  line-height: 1.7;
  overflow-x: hidden;
}


body { cursor: none; }
/* .cursor {
  position: fixed; width: 6px; height: 6px; border-radius: 50%;
  background: #C9A84C; pointer-events: none; z-index: 9999;
  transform: translate(-50%,-50%);
  transition: width .15s, height .15s, opacity .15s;
}
.IGNORE-ring {
  position: fixed; width: 32px; height: 32px; border-radius: 50%;
  border: 1px solid rgba(201,168,76,.35); pointer-events: none; z-index: 9998;
  transform: translate(-50%,-50%);
  transition: transform .08s ease, width .2s, height .2s;
}

/* ── NAV ── */
nav {
  position: fixed; top: 0; left: 0; right: 0; z-index: 100;
  display: flex; align-items: center; justify-content: space-between;
  padding: 0 64px; height: 64px;
  border-bottom: 1px solid rgba(201,168,76,0.16);
  background: rgba(13,27,42,0.97);
  backdrop-filter: blur(24px);
}
.nav-left { display: flex; align-items: center; gap: 16px; }
.nav-monogram {
  position: relative; width: 32px; height: 32px;
  font-family: 'Playfair Display', serif; font-weight: 800;
}
.nm-b {
  font-size: 1.7rem; color: #C9A84C; line-height: 1;
  position: absolute; top: 0; left: 0; opacity: 0.7;
}
.nm-f {
  font-size: 1.7rem; color: #C9A84C; line-height: 1;
  position: absolute; top: 3px; left: 6px;
}
.nav-wordmark {
  display: flex; flex-direction: column; gap: 1px;
}
.nav-name {
  font-family: 'Playfair Display', serif; font-weight: 700;
  font-size: 0.8rem; color: #F0EDE6; letter-spacing: 0.06em;
  line-height: 1;
}
.nav-name em { color: #C9A84C; font-style: normal; }
.nav-tagline {
  font-family: 'IBM Plex Mono', monospace; font-size: 0.42rem;
  color: rgba(201,168,76,0.72); letter-spacing: 0.18em;
  text-transform: uppercase; line-height: 1;
}
.nav-links { display: flex; gap: 40px; list-style: none; }
.nav-links a {
  font-family: 'IBM Plex Mono', monospace; font-size: 0.65rem;
  letter-spacing: 0.12em; text-transform: uppercase;
  color: rgba(240,237,230,0.72); text-decoration: none;
  transition: color 0.2s; position: relative;
}
.nav-links a::after {
  content: ''; position: absolute; bottom: -2px; left: 0; right: 0;
  height: 1px; background: #C9A84C;
  transform: scaleX(0); transition: transform 0.25s ease;
}
.nav-links a:hover { color: #C9A84C; }
.nav-links a:hover::after { transform: scaleX(1); }
.nav-cta {
  font-family: 'IBM Plex Mono', monospace; font-size: 0.62rem;
  letter-spacing: 0.1em; text-transform: uppercase;
  padding: 8px 20px;
  border: 1px solid rgba(201,168,76,0.4);
  color: #C9A84C; text-decoration: none;
  transition: all 0.2s;
}
.nav-cta:hover { background: #C9A84C; color: #000; }

/* ── HERO ── */
#hero {
  min-height: 100vh;
  display: flex; align-items: center;
  padding: 120px 64px 80px;
  position: relative; overflow: hidden;
  background: #0D1B2A;
}
/* Subtle grid overlay */
#hero::before {
  content: '';
  position: absolute; inset: 0;
  background-image:
    linear-gradient(rgba(201,168,76,0.04) 1px, transparent 1px),
    linear-gradient(90deg, rgba(201,168,76,0.04) 1px, transparent 1px);
  background-size: 80px 80px;
  z-index: 0;
}
/* Gold orb — top right */
#hero::after {
  content: '';
  position: absolute; top: -20%; right: -10%;
  width: 700px; height: 700px; border-radius: 50%;
  background: radial-gradient(circle, rgba(201,168,76,0.06) 0%, transparent 65%);
  z-index: 0;
}
.hero-inner {
  position: relative; z-index: 1;
  max-width: 900px;
}
.hero-kicker {
  font-family: 'IBM Plex Mono', monospace; font-size: 0.65rem;
  letter-spacing: 0.22em; text-transform: uppercase;
  color: #C9A84C; margin-bottom: 28px;
  display: flex; align-items: center; gap: 14px;
}
.hero-kicker::before {
  content: ''; width: 32px; height: 1px; background: #C9A84C; flex-shrink: 0;
}
h1 {
  font-family: 'Playfair Display', serif; font-weight: 800;
  font-size: clamp(3.5rem, 6vw, 6rem);
  line-height: 1.02; letter-spacing: -0.03em;
  color: #F0EDE6 !important; margin-bottom: 28px;
}
h1 em { color: #C9A84C; font-style: italic; }
.hero-body {
  font-size: 1.1rem; color: rgba(240,237,230,0.82);
  line-height: 1.82; max-width: 560px; margin-bottom: 44px;
  font-weight: 300;
}
.hero-actions { display: flex; gap: 14px; align-items: center; }
.btn-primary {
  font-family: 'IBM Plex Mono', monospace; font-size: 0.68rem;
  letter-spacing: 0.1em; text-transform: uppercase;
  padding: 14px 32px; background: #C9A84C; color: #000;
  text-decoration: none; border: 1px solid #C9A84C;
  transition: all 0.2s; font-weight: 500;
}
.btn-primary:hover { background: transparent; color: #C9A84C; }
.btn-text {
  font-family: 'IBM Plex Mono', monospace; font-size: 0.65rem;
  letter-spacing: 0.1em; text-transform: uppercase;
  color: rgba(240,237,230,0.62); text-decoration: none;
  transition: color 0.2s; display: flex; align-items: center; gap: 8px;
}
.btn-text:hover { color: #C9A84C; }
.btn-text::after { content: '→'; }
/* Hero scroll indicator */
.scroll-hint {
  position: absolute; bottom: 40px; left: 64px;
  font-family: 'IBM Plex Mono', monospace; font-size: 0.58rem;
  letter-spacing: 0.16em; text-transform: uppercase;
  color: rgba(240,237,230,0.4);
  display: flex; align-items: center; gap: 10px;
}
.scroll-hint::before {
  content: ''; width: 1px; height: 40px; background: rgba(201,168,76,0.3);
  animation: scrollLine 2s ease-in-out infinite;
}
@keyframes scrollLine {
  0%, 100% { opacity: 0.3; transform: scaleY(1); }
  50% { opacity: 1; transform: scaleY(1.2); }
}

/* ── SECTION BASE ── */
section { position: relative; }
.wrap { max-width: 1140px; margin: 0 auto; padding: 96px 64px; }

/* Thin gold rule between sections */
.rule {
  width: 100%; height: 1px;
  background: linear-gradient(90deg, transparent, rgba(201,168,76,0.25), transparent);
}

/* Section labels */
.label {
  font-family: 'IBM Plex Mono', monospace; font-size: 0.6rem;
  letter-spacing: 0.2em; text-transform: uppercase; color: #C9A84C;
  margin-bottom: 20px; display: block;
}

/* Section headings */
h2 {
  font-family: 'Playfair Display', serif; font-weight: 700;
  font-size: clamp(1.9rem, 3vw, 2.8rem);
  color: #F0EDE6 !important; line-height: 1.1; letter-spacing: -0.02em;
  margin-bottom: 14px;
}
h2 em { color: #C9A84C; font-style: italic; }
.sub {
  font-size: 1rem; color: rgba(240,237,230,0.68);
  line-height: 1.8; max-width: 580px;
  margin-bottom: 48px; font-weight: 300;
}

/* ── STATEMENT SECTION ── */
#statement { background: #0D1B2A; border-top: 1px solid rgba(201,168,76,0.14); }
.statement-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 80px; align-items: start; }
.statement-left {}
.statement-right { padding-top: 4px; }
.statement-body {
  font-size: 1.05rem; color: rgba(240,237,230,0.78);
  line-height: 1.88; margin-bottom: 18px; font-weight: 300;
}
.statement-body strong { color: #F0EDE6; font-weight: 500; }
.gold-bar {
  display: block; width: 32px; height: 2px; background: #C9A84C; margin-bottom: 28px;
}

/* ── FRAMEWORK ── */
#framework { background: #0D1B2A; border-top: 1px solid rgba(201,168,76,0.14); }
.fw-grid { display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 1px;
  background: rgba(201,168,76,0.12); margin-top: 48px; }
.fw-item { background: #0D1B2A; padding: 40px 32px; position: relative; overflow: hidden;
  transition: background 0.3s; }
.fw-item:hover { background: #162436; }
.fw-item::before {
  content: ''; position: absolute; top: 0; left: 0; right: 0; height: 1px;
  background: #C9A84C; transform: scaleX(0); transform-origin: left;
  transition: transform 0.35s ease;
}
.fw-item:hover::before { transform: scaleX(1); }
.fw-num {
  font-family: 'IBM Plex Mono', monospace; font-size: 0.58rem;
  letter-spacing: 0.16em; text-transform: uppercase; color: rgba(201,168,76,0.7);
  margin-bottom: 20px;
}
.fw-title {
  font-family: 'Playfair Display', serif; font-weight: 700;
  font-size: 1.6rem; color: #F0EDE6; margin-bottom: 12px; line-height: 1.1;
}
.fw-desc { font-size: 0.9rem; color: rgba(240,237,230,0.78); line-height: 1.78; margin-bottom: 24px; }
.fw-link {
  font-family: 'IBM Plex Mono', monospace; font-size: 0.6rem;
  letter-spacing: 0.1em; text-transform: uppercase; color: #C9A84C;
  text-decoration: none; display: flex; align-items: center; gap: 8px;
  transition: gap 0.2s;
}
.fw-link:hover { gap: 14px; }

/* ── PRINCIPLES ── */
#principles { background: #0D1B2A; border-top: 1px solid rgba(201,168,76,0.14); }
.principles-list { margin-top: 48px; }
.principle {
  display: grid; grid-template-columns: 80px 1fr 1fr;
  gap: 40px; padding: 32px 0;
  border-bottom: 1px solid rgba(240,237,230,0.08);
  align-items: start; transition: border-color 0.2s;
}
.principle:hover { border-bottom-color: rgba(201,168,76,0.2); }
.principle:first-child { border-top: 1px solid rgba(240,237,230,0.08); }
.p-num {
  font-family: 'IBM Plex Mono', monospace; font-weight: 500;
  font-size: 0.65rem; letter-spacing: 0.1em; color: rgba(201,168,76,0.6);
  padding-top: 4px;
}
.p-title {
  font-family: 'Playfair Display', serif; font-weight: 700;
  font-size: 1.15rem; color: #F0EDE6; line-height: 1.3;
}
.p-desc { font-size: 0.9rem; color: rgba(240,237,230,0.78); line-height: 1.75; }

/* ── MISSION STATEMENT ── */
#mission { background: #0D1B2A;
  border-top: 1px solid rgba(201,168,76,0.14);
  background: #000;
}
.mission-inner { max-width: 800px; }
.mission-quote {
  font-family: 'Playfair Display', serif; font-style: italic;
  font-size: clamp(1.4rem, 2.5vw, 2rem);
  color: #F0EDE6; line-height: 1.55; margin-bottom: 20px;
}
.mission-quote em { color: #C9A84C; }
.mission-attr {
  font-family: 'IBM Plex Mono', monospace; font-size: 0.6rem;
  letter-spacing: 0.16em; text-transform: uppercase;
  color: rgba(201,168,76,0.7);
  display: flex; align-items: center; gap: 12px;
}
.mission-attr::before { content: ''; width: 24px; height: 1px; background: rgba(201,168,76,0.4); }

/* ── STATS ── */
#stats { background: #0D1B2A; border-top: 1px solid rgba(201,168,76,0.14); }
.stats-row { display: grid; grid-template-columns: repeat(4,1fr); gap: 1px;
  background: rgba(201,168,76,0.12); margin-top: 48px; }
.stat { background: #0D1B2A; padding: 40px 28px; }
.stat-n {
  font-family: 'Playfair Display', serif; font-weight: 800;
  font-size: 3.2rem; color: #C9A84C; line-height: 1; margin-bottom: 8px;
}
.stat-n sup { font-size: 1.4rem; color: rgba(201,168,76,0.6); }
.stat-l {
  font-family: 'IBM Plex Mono', monospace; font-size: 0.6rem;
  letter-spacing: 0.08em; text-transform: uppercase;
  color: rgba(240,237,230,0.72); line-height: 1.5;
}
.stat-d { font-size: 0.85rem; color: rgba(240,237,230,0.45); margin-top: 6px; }

/* ── CLOSING ── */
#closing { background: #0D1B2A;
  border-top: 1px solid rgba(201,168,76,0.14);
  min-height: 50vh; display: flex; align-items: center;
}
.closing-inner { text-align: center; max-width: 700px; margin: 0 auto; }
.closing-line {
  font-family: 'Playfair Display', serif; font-style: italic;
  font-size: clamp(1.6rem, 3vw, 2.6rem);
  color: rgba(240,237,230,0.85); line-height: 1.4; margin-bottom: 6px;
}
.closing-line.gold { color: #C9A84C; margin-bottom: 40px; }

/* ── FOOTER ── */
footer {
  background: #081422;
  border-top: 1px solid rgba(201,168,76,0.12);
  padding: 28px 64px;
  display: flex; justify-content: space-between; align-items: center;
}
.footer-left { display: flex; align-items: center; gap: 20px; }
.footer-brand {
  font-family: 'Playfair Display', serif; font-weight: 700;
  font-size: 0.85rem; color: #F0EDE6; letter-spacing: 0.04em;
}
.footer-brand em { color: #C9A84C; font-style: normal; }
.footer-sep { width: 1px; height: 16px; background: rgba(201,168,76,0.2); }
.footer-note {
  font-family: 'IBM Plex Mono', monospace; font-size: 0.52rem;
  letter-spacing: 0.1em; text-transform: uppercase;
  color: rgba(240,237,230,0.4);
}


.palette-bar {
  position: fixed; bottom: 0; left: 0; right: 0;
  height: 3px; display: flex;
}
.pb-1 { flex: 3; background: #000000; }
.pb-2 { flex: 1; background: #C9A84C; }
.pb-3 { flex: 2; background: #F0EDE6; }
.pb-4 { flex: 1; background: #6B7280; }

/* ── REVEAL ── */
.reveal { opacity: 0; transform: translateY(16px); transition: opacity .7s ease, transform .7s ease; }
.reveal.in { opacity: 1; transform: none; }

/* ── MOBILE ── */
@media (max-width: 900px) {
  nav { padding: 0 22px; }
  .nav-links { display: none; }
  #hero { padding: 100px 24px 60px; }
  .wrap { padding: 64px 24px; }
  .statement-grid, .fw-grid, .stats-row { grid-template-columns: 1fr; gap: 0; }
  .principle { grid-template-columns: 48px 1fr; gap: 20px; }
  .p-desc { grid-column: 2; }
  footer { padding: 20px 24px; flex-direction: column; gap: 12px; text-align: center; }
}



/* ── ADDITIONAL SHARED STYLES ─────────────── */
body { cursor: default; }
.rule { width:100%; height:1px; background:linear-gradient(90deg,transparent,rgba(201,168,76,.22),transparent); }
.wrap { max-width:1140px; margin:0 auto; padding:96px 64px; }
.label { font-family:'IBM Plex Mono',monospace; font-size:.6rem; letter-spacing:.2em; text-transform:uppercase; color:#C9A84C; margin-bottom:20px; display:block; }
h2 { font-family:'Playfair Display',serif; font-weight:700; font-size:clamp(1.9rem,3vw,2.8rem); color:#F0EDE6 !important; line-height:1.1; letter-spacing:-.02em; margin-bottom:14px; }
h2 em { color:#C9A84C; font-style:italic; }
h3 { font-family:'Playfair Display',serif; font-weight:700; font-size:1.1rem; color:#F0EDE6; margin-bottom:6px; line-height:1.3; }
.sub { font-size:1rem; color:rgba(240,237,230,.68); line-height:1.8; max-width:580px; margin-bottom:48px; font-weight:300; }
.gold-bar { display:block; width:32px; height:2px; background:#C9A84C; margin-bottom:28px; }
/* Cards */
.cards-grid { display:grid; grid-template-columns:repeat(auto-fit,minmax(280px,1fr)); gap:1px; background:rgba(201,168,76,.1); }
.card-item { background:#0D1B2A; padding:32px 28px; transition:background .25s; }
.card-item:hover { background:#162436; }
.card-num { font-family:'IBM Plex Mono',monospace; font-size:.58rem; letter-spacing:.14em; text-transform:uppercase; color:rgba(201,168,76,.6); margin-bottom:10px; }
.card-title { font-family:'Playfair Display',serif; font-weight:700; font-size:1rem; color:#F0EDE6; margin-bottom:8px; line-height:1.3; }
.card-desc { font-size:.88rem; color:rgba(240,237,230,.65); line-height:1.75; }
/* Timeline */
.timeline { position:relative; padding-left:28px; }
.timeline::before { content:''; position:absolute; left:7px; top:0; bottom:0; width:1px; background:rgba(201,168,76,.2); }
.tl-item { position:relative; padding-bottom:36px; }
.tl-item:last-child { padding-bottom:0; }
.tl-item::before { content:''; position:absolute; left:-23px; top:6px; width:8px; height:8px; border-radius:50%; background:#C9A84C; box-shadow:0 0 0 3px #0D1B2A,0 0 0 4px rgba(201,168,76,.3); }
.tl-date { font-family:'IBM Plex Mono',monospace; font-size:.6rem; color:rgba(201,168,76,.7); letter-spacing:.1em; text-transform:uppercase; margin-bottom:4px; }
.tl-role { font-size:1rem; font-weight:600; color:#F0EDE6; margin-bottom:2px; }
.tl-org { font-size:.86rem; color:rgba(201,168,76,.7); margin-bottom:8px; }
.tl-desc { font-size:.88rem; color:rgba(240,237,230,.62); line-height:1.75; }
.tl-tags { display:flex; flex-wrap:wrap; gap:5px; margin-top:10px; }
.tl-tag { font-family:'IBM Plex Mono',monospace; font-size:.54rem; letter-spacing:.07em; text-transform:uppercase; padding:2px 9px; border:1px solid rgba(201,168,76,.2); color:rgba(201,168,76,.6); }
/* Checklist */
.checklist { list-style:none; display:flex; flex-direction:column; gap:10px; margin-top:16px; }
.checklist li { font-size:.92rem; color:rgba(240,237,230,.68); line-height:1.65; display:flex; gap:10px; align-items:baseline; }
.checklist li::before { content:'→'; color:#C9A84C; flex-shrink:0; font-size:.85rem; }
/* Steps */
.steps { display:flex; flex-direction:column; gap:1px; background:rgba(201,168,76,.1); margin-top:32px; }
.step { background:#0D1B2A; padding:28px 32px; display:grid; grid-template-columns:52px 1fr; gap:24px; align-items:start; transition:background .2s; }
.step:hover { background:#162436; }
.step-n { font-family:'Playfair Display',serif; font-weight:800; font-size:1.6rem; color:rgba(201,168,76,.35); line-height:1; padding-top:2px; }
.step h3 { color:#F0EDE6; margin-bottom:6px; }
.step p { font-size:.9rem; color:rgba(240,237,230,.62); line-height:1.75; }
.step ul { list-style:none; margin-top:10px; display:flex; flex-direction:column; gap:5px; }
.step ul li { font-size:.86rem; color:rgba(240,237,230,.55); display:flex; gap:8px; }
.step ul li::before { content:'—'; color:rgba(201,168,76,.5); flex-shrink:0; }
/* Mission block */
.mission-block { border-left:2px solid #C9A84C; padding:20px 24px; background:rgba(201,168,76,.04); }
.mission-block-title { font-family:'IBM Plex Mono',monospace; font-size:.58rem; letter-spacing:.14em; text-transform:uppercase; color:rgba(201,168,76,.7); margin-bottom:8px; }
.mission-block p { font-family:'Playfair Display',serif; font-style:italic; font-size:.97rem; color:rgba(240,237,230,.8); line-height:1.65; }
/* Two col */
.two-col { display:grid; grid-template-columns:1fr 1fr; gap:64px; }
.body-text { font-size:.97rem; color:rgba(240,237,230,.65); line-height:1.88; font-weight:300; }
.body-text + .body-text { margin-top:14px; }
.body-text strong { color:#F0EDE6; font-weight:500; }
/* Buttons */
.btns { display:flex; gap:12px; flex-wrap:wrap; }
.btn-primary { font-family:'IBM Plex Mono',monospace; font-size:.68rem; letter-spacing:.1em; text-transform:uppercase; padding:13px 30px; background:#C9A84C; color:#000; text-decoration:none; border:1px solid #C9A84C; transition:all .2s; }
.btn-primary:hover { background:transparent; color:#C9A84C; }
.btn-ghost { font-family:'IBM Plex Mono',monospace; font-size:.68rem; letter-spacing:.1em; text-transform:uppercase; padding:13px 30px; background:transparent; color:rgba(240,237,230,.7); border:1px solid rgba(240,237,230,.2); text-decoration:none; transition:all .2s; display:flex; align-items:center; gap:8px; }
.btn-ghost:hover { border-color:rgba(240,237,230,.5); color:#F0EDE6; }
.btn-ghost::after { content:'→'; }
/* Research cards */
.research-grid { display:grid; grid-template-columns:1fr 1fr; gap:1px; background:rgba(201,168,76,.1); margin-top:32px; }
.r-card { background:#0D1B2A; padding:28px 24px; position:relative; transition:background .25s; }
.r-card:hover { background:#162436; }
.r-card::before { content:''; position:absolute; top:0; left:0; right:0; height:1px; background:#C9A84C; transform:scaleX(0); transform-origin:left; transition:transform .3s ease; }
.r-card:hover::before { transform:scaleX(1); }
.r-label { font-family:'IBM Plex Mono',monospace; font-size:.55rem; letter-spacing:.12em; text-transform:uppercase; color:rgba(201,168,76,.65); margin-bottom:8px; }
.r-title { font-family:'Playfair Display',serif; font-weight:700; font-size:.97rem; color:#F0EDE6; margin-bottom:8px; line-height:1.3; }
.r-desc { font-size:.86rem; color:rgba(240,237,230,.58); line-height:1.72; margin-bottom:12px; }
.r-link { font-family:'IBM Plex Mono',monospace; font-size:.58rem; letter-spacing:.1em; text-transform:uppercase; color:rgba(201,168,76,.7); text-decoration:none; transition:color .2s; }
.r-link:hover { color:#C9A84C; }
/* Stat boxes */
.stat-row { display:grid; grid-template-columns:repeat(4,1fr); gap:1px; background:rgba(201,168,76,.1); }
.stat-box { background:#0D1B2A; padding:32px 24px; }
.stat-n { font-family:'Playfair Display',serif; font-weight:800; font-size:3rem; color:#C9A84C; line-height:1; margin-bottom:6px; }
.stat-n sup { font-size:1.3rem; color:rgba(201,168,76,.6); }
.stat-l { font-family:'IBM Plex Mono',monospace; font-size:.58rem; letter-spacing:.08em; text-transform:uppercase; color:rgba(240,237,230,.55); line-height:1.5; }
.stat-d { font-size:.85rem; color:rgba(240,237,230,.42); margin-top:5px; }
/* Section hero */
.s-hero { min-height:55vh; position:relative; overflow:hidden; display:flex; align-items:flex-end; padding:130px 64px 64px; background:#081422; }
.s-hero-img { position:absolute; top:0; left:0; width:100%; height:100%; background-size:cover; background-position:center; -webkit-background-size:cover; opacity:.14; z-index:0; }
.s-hero-ov { position:absolute; top:0; left:0; width:100%; height:100%; background:linear-gradient(135deg,rgba(8,20,34,.98) 0%,rgba(13,27,42,.88) 55%,rgba(13,27,42,.72) 100%); z-index:1; }
.s-hero-content { position:relative; z-index:2; max-width:1140px; margin:0 auto; width:100%; }
.s-kicker { font-family:'IBM Plex Mono',monospace; font-size:.62rem; letter-spacing:.2em; text-transform:uppercase; color:rgba(201,168,76,.75); margin-bottom:18px; display:flex; align-items:center; gap:12px; }
.s-kicker::before { content:''; width:28px; height:1px; background:rgba(201,168,76,.5); flex-shrink:0; }
/* Closing */
.page-close { text-align:center; padding:96px 40px; }
.cl1 { font-family:'Playfair Display',serif; font-style:italic; font-size:clamp(1.5rem,2.5vw,2.2rem); color:rgba(240,237,230,.82); line-height:1.4; margin-bottom:6px; }
.cl2 { font-family:'Playfair Display',serif; font-style:italic; font-size:clamp(1.5rem,2.5vw,2.2rem); color:#C9A84C; line-height:1.4; margin-bottom:36px; }
/* Comm cards */
.comm-grid { display:grid; grid-template-columns:repeat(auto-fit,minmax(220px,1fr)); gap:1px; background:rgba(201,168,76,.1); margin-top:32px; }
.comm-card { background:#0D1B2A; padding:26px 22px; transition:background .25s; }
.comm-card:hover { background:#162436; }
.comm-icon { font-family:'Playfair Display',serif; font-weight:800; font-size:1.5rem; color:#C9A84C; margin-bottom:10px; }
.comm-title { font-size:.92rem; font-weight:600; color:#F0EDE6; margin-bottom:5px; }
.comm-desc { font-size:.83rem; color:rgba(240,237,230,.55); line-height:1.65; }
/* Responsive */
@media(max-width:900px){
  nav{padding:0 18px;}
  .nav-links{display:none;}
  .wrap{padding:60px 22px;}
  .two-col,.research-grid{grid-template-columns:1fr;}
  .stat-row{grid-template-columns:1fr 1fr;}
  .s-hero{padding:100px 22px 52px;}
  .steps .step{grid-template-columns:36px 1fr;gap:14px;}
  footer{padding:20px 22px;flex-direction:column;gap:10px;text-align:center;}
}


</style>
</head>
<body>
<nav>
  <div class="nav-left">
    <div class="nav-monogram"><span class="nm-b">C</span><span class="nm-f">C</span></div>
    <div class="nav-wordmark">
      <div class="nav-name">CIPHER <em>&amp;</em> COUNSEL</div>
      <div class="nav-tagline">Cybersecurity · Policy · Law</div>
    </div>
  </div>
  <ul class="nav-links"><li><a href="aware.html"class="active">Aware</a></li><li><a href="protect.html">Protect</a></li><li><a href="advocate.html">Advocate</a></li><li><a href="about.html">About</a></li></ul>
  <a href="about.html" class="nav-cta">Meet the Founder</a>
</nav>

<div class="s-hero">
  <div class="s-hero-img" style="background-image:url('hero-bg-globe.png');"></div>
  <div class="s-hero-ov"></div>
  <div class="s-hero-content">
    <div class="s-kicker">Aware — Module 01</div>
    <h1 style="font-family:'Playfair Display',serif;font-weight:800;font-size:clamp(2.8rem,5vw,4.4rem);line-height:1.04;letter-spacing:-.03em;color:#F0EDE6 !important;margin-bottom:16px;">What Is a Digital<br><em style="color:#C9A84C;font-style:italic;">Footprint?</em></h1>
    <p style="font-size:1rem;color:rgba(240,237,230,.65);line-height:1.8;max-width:560px;font-weight:300;">Every action you take online leaves data behind. Your digital footprint is the trail of information you create, share, and leave exposed — whether you know it or not.</p>
  </div>
</div>

<div class="rule"></div>

<section style="background:#0D1B2A;">
  <div class="wrap">
    <span class="label reveal">The Three Types</span>
    <h2 class="reveal">Understanding your<br><em>digital trail</em></h2>
    <p class="sub reveal">Not all digital footprints are the same. Understanding the difference between active, passive, and persistent data is the foundation of awareness.</p>
    <div class="cards-grid">
      <div class="card-item reveal">
        <div class="card-num">Active — What you choose to share</div>
        <div class="card-title">Intentional Data</div>
        <div class="card-desc">Social media posts, online forms, profile information, comments, and anything you deliberately publish. You control this — but most people share far more than they intend to.</div>
      </div>
      <div class="card-item reveal">
        <div class="card-num">Passive — Collected without you knowing</div>
        <div class="card-title">Invisible Collection</div>
        <div class="card-desc">Browsing history, location data, device information, ad tracking pixels, and behavioral data collected silently in the background. You did not choose to share this — it was taken.</div>
      </div>
      <div class="card-item reveal">
        <div class="card-num">Persistent — Stays even after deletion</div>
        <div class="card-title">The Permanent Record</div>
        <div class="card-desc">Cached pages, data broker records, screenshots, archived content, and information already sold to third parties. Deletion does not equal erasure. Data outlives accounts.</div>
      </div>
    </div>
  </div>
</section>

<div class="rule"></div>

<section style="background:#0D1B2A;">
  <div class="wrap">
    <span class="label reveal">Exposure Sources</span>
    <h2 class="reveal">Where your<br><em>data comes from</em></h2>
    <p class="sub reveal">Your digital footprint is built across five primary sources — most operating in the background without your awareness.</p>
    <div style="display:flex;flex-direction:column;gap:1px;background:rgba(201,168,76,.1);margin-top:32px;">
      <div class="step reveal"><div class="step-n">01</div><div><h3>Location Services</h3><p>Your movements are tracked each time an app accesses your GPS. Many continue tracking in the background when you are not using them.</p></div></div>
      <div class="step reveal"><div class="step-n">02</div><div><h3>Apps and Platforms</h3><p>The average smartphone has 80+ apps. Many request access to camera, microphone, and contacts far beyond what they need to function.</p></div></div>
      <div class="step reveal"><div class="step-n">03</div><div><h3>Wi-Fi Connections</h3><p>The networks you join reveal your location patterns and browsing behavior. Public Wi-Fi networks are particularly risky for sensitive activity.</p></div></div>
      <div class="step reveal"><div class="step-n">04</div><div><h3>Search History</h3><p>Every query builds a profile of your interests, health concerns, and financial situation. This profile is sold to advertisers and retained indefinitely.</p></div></div>
      <div class="step reveal"><div class="step-n">05</div><div><h3>Advertisers and Data Brokers</h3><p>Third-party trackers follow you across websites. Data brokers compile and sell your personal information legally — without your knowledge.</p></div></div>
    </div>
  </div>
</section>

<div class="rule"></div>

<section style="background:#081422;">
  <div class="wrap page-close">
    <p class="cl1 reveal">Awareness is power.</p>
    <p class="cl2 reveal">Stay aware. Stay protected.</p>
    <div class="btns reveal" style="justify-content:center;">
      <a href="protect.html" class="btn-primary">Next: Protect →</a>
      <a href="index.html" class="btn-ghost">Back to Home</a>
    </div>
  </div>
</section>
<footer>
  <div class="footer-left">
    <div class="footer-brand">CIPHER <em>&amp;</em> COUNSEL</div>
    <div class="footer-sep"></div>
    <div class="footer-note">Cybersecurity · Policy · Law · Est. 2025</div>
  </div>
  <div class="footer-note">cipherandcounsel.github.io</div>
</footer>
<script>
const io=new IntersectionObserver(e=>{e.forEach(x=>{if(x.isIntersecting)x.target.classList.add('in');});},{threshold:0.1});
document.querySelectorAll('.reveal').forEach(el=>io.observe(el));
</script>
</body>
</html>
